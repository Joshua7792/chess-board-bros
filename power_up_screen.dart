import 'package:flutter/material.dart';

class PowerUpScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Power-Ups'),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // Display available power-ups here
            // Implement UI elements to use power-ups
          ],
        ),
      ),
    );
  }
}
